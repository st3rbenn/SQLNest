node_modules

# Package managers
pnpm-debug.log*
.pnpm-store
package-lock.json

# build outputs
build
dist

# secrets / local env (garder .env.example)
.env
.env.local
.env.prod
!.env.example
!.env.prod.example

# deploy script contient chemins/URLs serveur — gitignored
/deploy.sh

# mac files
.DS_Store
